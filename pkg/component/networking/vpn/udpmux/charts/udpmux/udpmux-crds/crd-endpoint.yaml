apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/resource-policy": keep
  labels:
    app: udp-mux
    gardener.cloud/role: system-component
  name: endpoints.vpn.networking.gardener.cloud
spec:
  group: vpn.networking.gardener.cloud
  names:
    categories:
    - vpn-endpoint
    - networking-gardener-cloud
    kind: Endpoint
    listKind: EndpointList
    plural: endpoints
    shortNames:
    - ep
    singular: endpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The destination address of the endpoint
      jsonPath: .spec.addr
      name: Address
      type: string
    - description: CreationTimestamp is a timestamp representing the server time when
        this object was created. It is not guaranteed to be set in happens-before
        order across separate operations. Clients may not set this value. It is represented
        in RFC3339 form and is in UTC. Populated by the system. Read-only. Null for
        lists. For more information, see [Kubernetes API Conventions](https://github.com/kubernetes/community/blob/master/contributors/devel/sig-architecture/api-conventions.md#metadata)
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            description: 'Configuration affecting load balancing, outlier detection,
              etc. See more details at: https://istio.io/docs/reference/config/networking/destination-rule.html'
            properties:
              addr:
                description: The destination address of the endpoint
                type: string
    served: true
    storage: true
